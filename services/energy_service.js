'use strict';
function parseExerciseText(text){ const safe=String(text||'').trim(); let minutes=0; const minuteMatch=safe.match(/(\d+)\s*分/); if(minuteMatch) minutes=Number(minuteMatch[1]); if(/スクワット/.test(safe)&&!minutes) minutes=10; if(/ジョギング|ランニング/.test(safe)&&!minutes) minutes=20; if(/歩いた/.test(safe)&&!minutes) minutes=20; let kcal=0; if(/スクワット/.test(safe)) kcal=minutes*5; else if(/ジョギング|ランニング/.test(safe)) kcal=minutes*8; else if(/歩いた/.test(safe)) kcal=minutes*3.5; else if(/筋トレ/.test(safe)) kcal=minutes*4.5; return { minutes, estimatedKcal: Math.round(kcal) }; }
module.exports = { parseExerciseText };
